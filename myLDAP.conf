# myLDAP Configuration File
# Simple key=value format, one per line
# Lines starting with # are comments
# Whitespace around = is trimmed

# Server settings
server_port=1389
server_bind_ip=0.0.0.0

# Logging
log_file=server.log

# Database connection
db_host=localhost
db_port=3306
db_name=ldap_users
db_user=ldap
db_pass=ldap
